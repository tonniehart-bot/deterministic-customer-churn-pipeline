# deterministic-customer-churn-pipeline
This project implements a fully reproducible, end-to-end customer churn prediction pipeline designed to simulate real-world telecom analytics workflows. The system processes structured customer data through data cleaning, feature engineering, model training, and evaluation stages, ensuring deterministic outputs across runs. 
